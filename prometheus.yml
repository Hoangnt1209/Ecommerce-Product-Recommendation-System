global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "prometheus/alert_rules.yml"

scrape_configs:
  - job_name: 'recommendation_service'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['web-api:8000', 'localhost:8000']
